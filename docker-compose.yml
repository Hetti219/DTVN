services:
  # Bootstrap/Primary Node
  validator-0:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-0
    hostname: validator-0
    environment:
      - NODE_ID=validator-0
      - LISTEN_PORT=4001
      - API_PORT=8080
      - DATA_DIR=/home/validator/data
      - TOTAL_NODES=7
      - IS_PRIMARY=true
    ports:
      - "4001:4001"
      - "8080:8080"
      - "9090:9090"
    volumes:
      - validator-0-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  # Validator Node 1
  validator-1:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-1
    hostname: validator-1
    environment:
      - NODE_ID=validator-1
      - LISTEN_PORT=4001
      - API_PORT=8080
      - DATA_DIR=/home/validator/data
      - TOTAL_NODES=7
    ports:
      - "4002:4001"
      - "8081:8080"
      - "9091:9090"
    volumes:
      - validator-1-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      validator-0:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Validator Node 2
  validator-2:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-2
    hostname: validator-2
    command:
      - "-id"
      - "validator-2"
      - "-port"
      - "4001"
      - "-api-port"
      - "8080"
      - "-data-dir"
      - "/home/validator/data"
      - "-total-nodes"
      - "7"
      - "-bootstrap"
      - "/ip4/validator-0/tcp/4001"
    ports:
      - "4003:4001"
      - "8082:8080"
      - "9092:9090"
    volumes:
      - validator-2-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      validator-0:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Validator Node 3
  validator-3:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-3
    hostname: validator-3
    command:
      - "-id"
      - "validator-3"
      - "-port"
      - "4001"
      - "-api-port"
      - "8080"
      - "-data-dir"
      - "/home/validator/data"
      - "-total-nodes"
      - "7"
      - "-bootstrap"
      - "/ip4/validator-0/tcp/4001"
    ports:
      - "4004:4001"
      - "8083:8080"
      - "9093:9090"
    volumes:
      - validator-3-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      validator-0:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Validator Node 4
  validator-4:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-4
    hostname: validator-4
    command:
      - "-id"
      - "validator-4"
      - "-port"
      - "4001"
      - "-api-port"
      - "8080"
      - "-data-dir"
      - "/home/validator/data"
      - "-total-nodes"
      - "7"
      - "-bootstrap"
      - "/ip4/validator-0/tcp/4001"
    ports:
      - "4005:4001"
      - "8084:8080"
      - "9094:9090"
    volumes:
      - validator-4-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      validator-0:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Validator Node 5
  validator-5:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-5
    hostname: validator-5
    command:
      - "-id"
      - "validator-5"
      - "-port"
      - "4001"
      - "-api-port"
      - "8080"
      - "-data-dir"
      - "/home/validator/data"
      - "-total-nodes"
      - "7"
      - "-bootstrap"
      - "/ip4/validator-0/tcp/4001"
    ports:
      - "4006:4001"
      - "8085:8080"
      - "9095:9090"
    volumes:
      - validator-5-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      validator-0:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Validator Node 6
  validator-6:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: "${VERSION:-dev}"
        COMMIT: "${COMMIT:-unknown}"
        BUILD_DATE: "${BUILD_DATE:-unknown}"
    container_name: dtvn-validator-6
    hostname: validator-6
    command:
      - "-id"
      - "validator-6"
      - "-port"
      - "4001"
      - "-api-port"
      - "8080"
      - "-data-dir"
      - "/home/validator/data"
      - "-total-nodes"
      - "7"
      - "-bootstrap"
      - "/ip4/validator-0/tcp/4001"
    ports:
      - "4007:4001"
      - "8086:8080"
      - "9096:9090"
    volumes:
      - validator-6-data:/home/validator/data
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      validator-0:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # Prometheus for metrics collection
  prometheus:
    image: prom/prometheus:v3.1.0
    container_name: dtvn-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    ports:
      - "9999:9090"
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - dtvn-network
    restart: unless-stopped
    user: "65534:65534"  # nobody user for security

  # Grafana for metrics visualization
  grafana:
    image: grafana/grafana:11.5.1
    container_name: dtvn-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SECURITY_ADMIN_USER=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_INSTALL_PLUGINS=
    volumes:
      - grafana-data:/var/lib/grafana
      - ./config/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./config/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    networks:
      - dtvn-network
    restart: unless-stopped
    depends_on:
      - prometheus
    user: "472:0"  # grafana user for security

volumes:
  validator-0-data:
  validator-1-data:
  validator-2-data:
  validator-3-data:
  validator-4-data:
  validator-5-data:
  validator-6-data:
  prometheus-data:
  grafana-data:

networks:
  dtvn-network:
    driver: bridge
