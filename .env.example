# DTVN Environment Variables
# Copy this file to .env and fill in your values

# API Key for authentication (optional - leave empty for no auth)
# Generate with: openssl rand -hex 32
DTVN_API_KEY=

# Seed for deterministic peer key generation (required for production)
# Generate with: openssl rand -hex 32 or openssl rand -hex 64
DTVN_KEY_SEED=

# Additional WebSocket origins (optional - comma-separated)
# Example: DTVN_WS_ORIGINS=https://example.com,https://app.example.com
# DTVN_WS_ORIGINS=
